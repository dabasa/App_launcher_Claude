# Ph.10 実装指示書 ─ タスクトレイ・設定モード・仕上げ

## 実装前確認

| 項目 | 内容 |
|---|---|
| 対象フェーズ | Ph.10 タスクトレイ・設定モード・仕上げ |
| 実装目的 | 設定モード（3ループ遷移・仮想ページ・全体設定画面）、タスクトレイ、SnapService 4方向対応、自動起動、仕上げ |
| 未確定事項 | なし |
| 既存影響 | `AppMode` enum 拡張、`LauncherViewModel.ToggleMode` 変更（Edit→Settings）、`SnapService` 大幅変更（4方向）、`App.xaml.cs` にサービス追加 |
| 追加ライブラリ | なし（既存パッケージで対応） |
| 実装範囲 | 設定モード実装 / 全体設定 UI / タスクトレイ / 4方向吸着 / マルチモニター / 自動起動 / タイル縮小制限 / Debug 初期タイル |

---

## 対象ファイル一覧

| ファイル | 役割 |
|---|---|
| `Models/AppMode.cs` | 変更：`Settings` / `GlobalSettings` / `PageEdit` 追加 |
| `Models/Config/GlobalConfig.cs` | 変更：`SavedWindowTop?` / `SavedWindowLeft?` 追加 |
| `ViewModels/LauncherViewModel.cs` | 変更：3ループ遷移・仮想設定ページ・GlobalSettings / PageEdit コマンド |
| `ViewModels/GlobalSettingsViewModel.cs` | 新規：全体設定項目の編集 ViewModel |
| `ViewModels/PageEditViewModel.cs` | 新規：ページ編集項目の編集 ViewModel |
| `Views/Controls/GlobalSettingsView.xaml` | 新規：全体設定 UI |
| `Views/Controls/GlobalSettingsView.xaml.cs` | 新規：UI 配色適用 |
| `Views/Controls/PageEditView.xaml` | 新規：ページ編集 UI |
| `Views/Controls/PageEditView.xaml.cs` | 新規：UI 配色適用 |
| `Views/LauncherWindow.xaml` | 変更：`DisplayPage` バインディング・GlobalSettings / PageEdit パネル追加 |
| `Views/LauncherWindow.xaml.cs` | 変更：GlobalSettings / PageEdit パネル表示切替 |
| `Views/Controls/BottomBarControl.xaml.cs` | 変更：設定モード時の表示切替・インジケーター・編集モードは PageEdit ボタンのみ |
| `Views/Controls/PageNameControl.xaml.cs` | 変更：設定モード時はインライン編集不可 |
| `Services/TileLaunchService.cs` | 変更：`settings://` タイルのクリック処理 |
| `Services/SnapService.cs` | 変更：4方向吸着・マルチモニター・設定モード収納禁止・位置保存・各種バグ修正 |
| `Services/TrayIconService.cs` | 新規：タスクトレイアイコン・コンテキストメニュー |
| `App.xaml` | 変更：`ShutdownMode="OnExplicitShutdown"` |
| `App.xaml.cs` | 変更：TrayIconService 初期化・Alt+F4 対応 |

---

## 実装仕様

### 1. AppMode の拡張

`Models/AppMode.cs` に `Settings` / `GlobalSettings` / `PageEdit` を追加する。

```csharp
namespace AppLauncher.Models;

public enum AppMode { Normal, Edit, TileEdit, PageEdit, Settings, GlobalSettings }
```

---

### 2. LauncherViewModel の変更

#### 2-1. 3ループ遷移

`ToggleMode()` の分岐を以下の通り変更する。

```csharp
[RelayCommand]
private void ToggleMode()
{
    switch (Mode)
    {
        case AppMode.Normal:
            _pinnedBeforeEdit = IsPinned;
            Mode     = AppMode.Edit;
            IsPinned = true;
            break;
        case AppMode.Edit:
            Mode     = AppMode.Settings;
            IsPinned = true;       // 設定モードも自動ピン
            break;
        case AppMode.Settings:
            Mode     = AppMode.Normal;
            IsPinned = _pinnedBeforeEdit;
            break;
    }
}
```

#### 2-2. 仮想設定ページの生成

設定タイルを定義して `PageViewModel` を動的生成する。`AppConfig` の `TileCountCols` から 1 行あたりの最大タイル数を求め、複数ページになる場合はページを分割する。

追加フィールド：

```csharp
private int _settingsPageIndex;
public int SettingsPageIndex
{
    get => _settingsPageIndex;
    set { _settingsPageIndex = Math.Clamp(value, 0, SettingsPages.Count - 1); OnPropertyChanged(); OnPropertyChanged(nameof(DisplayPage)); }
}

// 設定タイル定義 ─ 追加のたびここにエントリを足す
private static readonly (string title, string path)[] SettingsTileDefs =
[
    ("全体設定", "settings://globalSettings"),
];
```

設定ページ群を生成するヘルパー：

```csharp
public List<PageViewModel> SettingsPages { get; } = [];

private void BuildSettingsPages()
{
    SettingsPages.Clear();
    int cols = App.ConfigService.Current.Global.TileCountCols;
    int rows = App.ConfigService.Current.Global.TileCountRows;
    int tilesPerPage = cols * rows;

    for (int start = 0; start < SettingsTileDefs.Length; start += tilesPerPage)
    {
        var pageConfig = new PageConfig { Name = "詳細設定" };
        for (int i = start; i < Math.Min(start + tilesPerPage, SettingsTileDefs.Length); i++)
        {
            var (title, path) = SettingsTileDefs[i];
            int localIdx = i - start;
            pageConfig.Tiles.Add(new TileConfig
            {
                Col      = localIdx % cols,
                Row      = localIdx / cols,
                ColSpan  = 1,
                RowSpan  = 1,
                Type     = "settings",  // 専用タイプ
                Title    = title,
                Path     = path,
                Color    = "gray",
                Opacity  = 0,
                FontSizePt = 16,
                FontColor  = "white",
            });
        }
        SettingsPages.Add(new PageViewModel(pageConfig));
    }
}
```

コンストラクタの末尾で `BuildSettingsPages()` を呼ぶ。

#### 2-3. DisplayPage プロパティ

`TileGridControl` のバインディング元として使用する。

```csharp
public PageViewModel DisplayPage =>
    Mode is AppMode.Settings or AppMode.GlobalSettings
        ? SettingsPages[Math.Clamp(SettingsPageIndex, 0, SettingsPages.Count - 1)]
        : CurrentPage;
```

`Mode` 変更時と `CurrentPageIndex` 変更時に `OnPropertyChanged(nameof(DisplayPage))` を発火させる。`Mode` は `[ObservableProperty]` でバッキングフィールドが生成されるため、`OnModeChanged` partial メソッドをオーバーライドする。

```csharp
partial void OnModeChanged(AppMode value)
{
    OnPropertyChanged(nameof(DisplayPage));
}
```

#### 2-4. GlobalSettings コマンド

```csharp
[ObservableProperty]
private GlobalSettingsViewModel? _globalSettingsVm;

[RelayCommand]
private void OpenGlobalSettings()
{
    GlobalSettingsVm = new GlobalSettingsViewModel();
    Mode = AppMode.GlobalSettings;
}

[RelayCommand]
private void ConfirmGlobalSettings()
{
    if (GlobalSettingsVm == null) return;
    GlobalSettingsVm.ApplyToConfig();
    App.ConfigService.Save();
    GlobalSettingsVm = null;
    BuildSettingsPages();           // タイル数変更に備えて再生成
    OnPropertyChanged(nameof(DisplayPage));
    Mode = AppMode.Settings;
}

[RelayCommand]
private void CloseGlobalSettings()
{
    GlobalSettingsVm = null;
    Mode = AppMode.Settings;
}
```

#### 2-5. 設定ページナビゲーション

```csharp
[RelayCommand]
private void NavigateToSettingsPage(int index)
{
    SettingsPageIndex = index;
    OnPropertyChanged(nameof(DisplayPage));
}
```

---

### 3. GlobalSettingsViewModel（新規）

`ViewModels/GlobalSettingsViewModel.cs` を新規作成する。

```csharp
using CommunityToolkit.Mvvm.ComponentModel;
using AppLauncher.Models.Config;

namespace AppLauncher.ViewModels;

public partial class GlobalSettingsViewModel : ObservableObject
{
    [ObservableProperty] private bool   _taskTrayResident;
    [ObservableProperty] private bool   _startWithWindows;
    [ObservableProperty] private int    _tileCountCols;
    [ObservableProperty] private int    _tileCountRows;
    [ObservableProperty] private int    _tileMargin;
    [ObservableProperty] private int    _pageNameFontSizePt;
    [ObservableProperty] private string _snapPosition = "right";
    [ObservableProperty] private int    _snapMonitor;
    [ObservableProperty] private int    _storageDelayMs;

    // 吸着方向の選択肢
    public string[] SnapPositions { get; } = ["right", "left", "top", "bottom"];

    // 接続モニター数（1〜実装中は 1 固定、マルチモニター実装後に動的変更）
    public int MaxMonitor { get; } = 1;

    public GlobalSettingsViewModel()
    {
        var g = App.ConfigService.Current.Global;
        var l = App.ConfigService.Current.Layout;
        TaskTrayResident  = g.TaskTrayResident;
        StartWithWindows  = g.StartWithWindows;
        TileCountCols     = g.TileCountCols;
        TileCountRows     = g.TileCountRows;
        TileMargin        = l.TileMargin;
        PageNameFontSizePt = g.PageNameFontSizePt;
        SnapPosition      = g.SnapPosition;
        SnapMonitor       = g.SnapMonitor;
        StorageDelayMs    = g.StorageDelayMs;
    }

    public void ApplyToConfig()
    {
        var g = App.ConfigService.Current.Global;
        var l = App.ConfigService.Current.Layout;
        g.TaskTrayResident  = TaskTrayResident;
        g.StartWithWindows  = StartWithWindows;
        g.TileCountCols     = TileCountCols;
        g.TileCountRows     = TileCountRows;
        l.TileMargin        = TileMargin;
        g.PageNameFontSizePt = PageNameFontSizePt;
        g.SnapPosition      = SnapPosition;
        g.SnapMonitor       = SnapMonitor;
        g.StorageDelayMs    = StorageDelayMs;

        // 自動起動レジストリ反映
        StartupRegistryHelper.Apply(StartWithWindows);

        // SnapService に吸着方向変更を通知
        App.SnapService?.ApplySnap();
    }
}
```

**タイル配置数の縮小制限**（`ApplyToConfig` 内で適用前にチェック）：

```csharp
// 縮小制限チェック：配置済みタイルの最大座標を下回れない
int minCols = App.ConfigService.Current.Pages
    .SelectMany(p => p.Tiles)
    .Select(t => t.Col + t.ColSpan)
    .DefaultIfEmpty(1).Max();
int minRows = App.ConfigService.Current.Pages
    .SelectMany(p => p.Tiles)
    .Select(t => t.Row + t.RowSpan)
    .DefaultIfEmpty(1).Max();
g.TileCountCols = Math.Max(TileCountCols, minCols);
g.TileCountRows = Math.Max(TileCountRows, minRows);
```

---

### 4. GlobalSettingsView（新規）

`Views/Controls/GlobalSettingsView.xaml` を新規作成する。TileEditControl と同じ「Frame 全面オーバーレイ」方式。

```xml
<UserControl x:Class="AppLauncher.Views.Controls.GlobalSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- ヘッダー（モード名） -->
            <RowDefinition Height="*"/>     <!-- スクロール設定フォーム -->
            <RowDefinition Height="Auto"/>  <!-- OK / キャンセル -->
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <TextBlock Grid.Row="0" x:Name="ModeLabel"
                   Text="全体設定" FontSize="14" FontWeight="Bold"
                   Foreground="White" HorizontalAlignment="Right"
                   Margin="0,12,16,8"/>

        <!-- 設定フォーム -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,10,0">
            <StackPanel Margin="16,0,16,8" DataContext="{Binding GlobalSettingsVm}">

                <!-- タスクトレイ常駐 -->
                <CheckBox Content="タスクトレイ常駐" IsChecked="{Binding TaskTrayResident}"
                          Foreground="White" Margin="0,8,0,4"/>

                <!-- Windows 起動時の自動起動 -->
                <CheckBox Content="Windows 起動時に自動起動" IsChecked="{Binding StartWithWindows}"
                          Foreground="White" Margin="0,4,0,8"/>

                <!-- タイル配置数 -->
                <TextBlock Text="タイル配置数" Foreground="White" Margin="0,8,0,4"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="横：" Foreground="White" VerticalAlignment="Center"/>
                    <Slider Minimum="4" Maximum="16" Value="{Binding TileCountCols}"
                            Width="120" VerticalAlignment="Center" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding TileCountCols}" Foreground="White"
                               Width="24" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    <TextBlock Text="縦：" Foreground="White" VerticalAlignment="Center" Margin="16,0,0,0"/>
                    <Slider Minimum="4" Maximum="16" Value="{Binding TileCountRows}"
                            Width="120" VerticalAlignment="Center" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding TileCountRows}" Foreground="White"
                               Width="24" VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>

                <!-- タイル間マージン -->
                <TextBlock Text="タイル間マージン（px）" Foreground="White" Margin="0,8,0,4"/>
                <StackPanel Orientation="Horizontal">
                    <Slider Minimum="0" Maximum="30" Value="{Binding TileMargin}"
                            Width="200" VerticalAlignment="Center" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding TileMargin}" Foreground="White"
                               Width="24" VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>

                <!-- ページ名フォントサイズ -->
                <TextBlock Text="ページ名フォントサイズ（pt）" Foreground="White" Margin="0,8,0,4"/>
                <StackPanel Orientation="Horizontal">
                    <Slider Minimum="6" Maximum="72" Value="{Binding PageNameFontSizePt}"
                            Width="200" VerticalAlignment="Center" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding PageNameFontSizePt}" Foreground="White"
                               Width="24" VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>

                <!-- 吸着位置 -->
                <TextBlock Text="吸着位置" Foreground="White" Margin="0,8,0,4"/>
                <ComboBox ItemsSource="{Binding SnapPositions}"
                          SelectedItem="{Binding SnapPosition}"
                          Width="120" HorizontalAlignment="Left"/>

                <!-- 収納待ち時間 -->
                <TextBlock Text="収納待ち時間（ms）" Foreground="White" Margin="0,8,0,4"/>
                <StackPanel Orientation="Horizontal">
                    <Slider Minimum="0" Maximum="3000" Value="{Binding StorageDelayMs}"
                            Width="200" VerticalAlignment="Center" TickFrequency="100" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding StorageDelayMs}" Foreground="White"
                               Width="32" VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>

        <!-- OK / キャンセル -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,16,12">
            <Border x:Name="OkButton" Width="80" Height="32" Margin="0,0,8,0"
                    CornerRadius="6" Cursor="Hand">
                <TextBlock Text="OK" Foreground="White"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <Border x:Name="CancelButton" Width="80" Height="32"
                    CornerRadius="6" Cursor="Hand">
                <TextBlock Text="キャンセル" Foreground="White"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
```

`GlobalSettingsView.xaml.cs`:

```csharp
using System.Windows;
using System.Windows.Controls;
using System.Windows.Input;
using AppLauncher.Helpers;
using AppLauncher.Models;
using AppLauncher.ViewModels;

namespace AppLauncher.Views.Controls;

public partial class GlobalSettingsView : UserControl
{
    public GlobalSettingsView()
    {
        InitializeComponent();
        OkButton.MouseLeftButtonUp     += (_, _) =>
            App.LauncherViewModel?.ConfirmGlobalSettingsCommand.Execute(null);
        CancelButton.MouseLeftButtonUp += (_, _) =>
            App.LauncherViewModel?.CloseGlobalSettingsCommand.Execute(null);
        Loaded += OnLoaded;
    }

    private void OnLoaded(object sender, RoutedEventArgs e) => ApplyUiElementColor();

    public void ApplyUiElementColor()
    {
        var page = App.LauncherViewModel?.CurrentPage;
        if (page == null) return;
        var colors = ColorHelper.ComputeUiElementColor(page.BackgroundColor, page.BackgroundOpacity);
        Background = colors.Background;
        OkButton.Background     = colors.Accent;
        CancelButton.Background = colors.Dim;
    }
}
```

---

### 5. LauncherWindow の変更

#### 5-1. TileGridControl のバインディング変更

`LauncherWindow.xaml` の TileGridControl を `DisplayPage` へバインドし直す。

```xml
<!-- 変更前 -->
<controls:TileGridControl Grid.Row="3" Margin="20,0"
                          DataContext="{Binding CurrentPage}"/>

<!-- 変更後 -->
<controls:TileGridControl Grid.Row="3" Margin="20,0"
                          DataContext="{Binding DisplayPage}"/>
```

#### 5-2. GlobalSettingsView パネルの追加

`TileEditPanel` の直下に追加する。

```xml
<!-- 全体設定パネル（GlobalSettings モード時に Frame 内全体に表示） -->
<controls:GlobalSettingsView x:Name="GlobalSettingsPanel"
                              Grid.RowSpan="7"
                              Visibility="Collapsed"
                              Panel.ZIndex="50"/>
```

#### 5-3. LauncherWindow.xaml.cs の変更

`SetViewModel()` の `PropertyChanged` ハンドラに GlobalSettings 分岐を追加する。

```csharp
if (e.PropertyName == nameof(LauncherViewModel.Mode))
{
    bool isTileEdit = vm.Mode == AppMode.TileEdit;
    TileEditPanel.Visibility = isTileEdit ? Visibility.Visible : Visibility.Collapsed;
    if (isTileEdit)
        TileEditPanel.ApplyUiElementColor();

    bool isGlobalSettings = vm.Mode == AppMode.GlobalSettings;
    GlobalSettingsPanel.Visibility = isGlobalSettings ? Visibility.Visible : Visibility.Collapsed;
    if (isGlobalSettings)
        GlobalSettingsPanel.ApplyUiElementColor();
}
```

---

### 6. BottomBarControl の変更（設定モード対応）

`Rebuild()` を以下のように変更する。

```csharp
private void Rebuild()
{
    if (_vm == null) return;
    var layout = App.ConfigService.Current.Layout;

    bool isEdit     = _vm.Mode == AppMode.Edit;
    bool isSettings = _vm.Mode is AppMode.Settings or AppMode.GlobalSettings;

    // ─── モードボタン色 ─────────────────────────────────────────────
    System.Windows.Media.Color modeColor;
    if (isEdit)
        modeColor = ColorPalette.GetColor("orange");
    else if (isSettings)
        modeColor = ColorPalette.GetColor("teal");   // 設定モードは teal
    else
        modeColor = ColorPalette.GetColor(_vm.CurrentPage.BackgroundColor);

    ModeButton.Background   = new SolidColorBrush(modeColor);
    ModeButton.CornerRadius = new CornerRadius(layout.SettingsButtonCornerRadius);

    // ─── 行2 は Edit モードのみ表示 ──────────────────────────────────
    PageMgmtRow.Visibility = isEdit ? Visibility.Visible : Visibility.Collapsed;
    if (isEdit) UpdatePageMgmtButtons();

    // ─── インジケーター再構築 ─────────────────────────────────────────
    RebuildIndicators();
}
```

`RebuildIndicators()` は設定モード時に `SettingsPages` を参照するよう変更する。

```csharp
private void RebuildIndicators()
{
    if (_vm == null) return;
    IndicatorPanel.Children.Clear();

    bool isSettings = _vm.Mode is AppMode.Settings or AppMode.GlobalSettings;
    var pages = isSettings ? _vm.SettingsPages : _vm.Pages;
    int currentIndex = isSettings ? _vm.SettingsPageIndex : _vm.CurrentPageIndex;

    for (int i = 0; i < pages.Count; i++)
    {
        // ... 既存の indicator 生成ロジック ...
        // NavigateToPageCommand → isSettings の場合は NavigateToSettingsPageCommand
        int pageIndex = i;
        indicator.MouseLeftButtonUp += (_, _) =>
        {
            if (isSettings)
                _vm.NavigateToSettingsPageCommand.Execute(pageIndex);
            else
                _vm.NavigateToPageCommand.Execute(pageIndex);
        };
        // isCurrent の比較は currentIndex を使う
        bool isCurrent = i == currentIndex;
        // ... 既存の Ellipse 生成 ...
    }
}
```

---

### 7. PageNameControl の変更（設定モード時はインライン編集不可）

既存の `PageNameControl.xaml.cs` 内で編集開始をトリガーする箇所（`MouseLeftButtonUp` など）を、設定モード時は無視するよう変更する。

```csharp
// 編集開始の条件に Settings モードを追加
private void OnTextLabelClick(object sender, MouseButtonEventArgs e)
{
    var mode = App.LauncherViewModel?.Mode;
    if (mode != AppMode.Edit) return;   // Edit のみ編集可（Settings は不可）
    // ... 既存の編集開始処理 ...
}
```

また、編集確定のタイミングでモード遷移時（`Settings` への遷移含む）に確定するよう既存の `OnViewModelModeChanged` ハンドラを確認・修正する。

---

### 8. TileLaunchService の変更（settings:// 対応）

`TileLaunchService.Launch()` の冒頭に設定タイルの分岐を追加する。

```csharp
public static void Launch(TileViewModel tile)
{
    // 設定タイルの処理（settings:// スキーム）
    if (tile.Type == "settings")
    {
        HandleSettingsTile(tile.Path);
        return;
    }
    // ... 既存の処理 ...
}

private static void HandleSettingsTile(string path)
{
    switch (path)
    {
        case "settings://globalSettings":
            App.LauncherViewModel?.OpenGlobalSettingsCommand.Execute(null);
            break;
        // 将来の設定タイルはここに追加
    }
}
```

---

### 9. SnapService の拡張

#### 9-1. 設定モード時の収納禁止

`OnWindowMouseLeave()` と `StorageTimerTick()` の判定に `Settings` / `GlobalSettings` を追加する。

```csharp
private void OnWindowMouseLeave(object sender, MouseEventArgs e)
{
    var vm = App.LauncherViewModel;
    // 設定モード・全体設定モード中は収納しない
    if (vm == null || vm.IsPinned || vm.IsStored || _isAnimating) return;
    if (vm.Mode is AppMode.Settings or AppMode.GlobalSettings) return;
    if (IsMouseInWindowOrGap()) return;
    StartStorageTimer();
}
```

同様に `StorageTimerTick()` にも同じ判定を追加する。

#### 9-2. 4方向吸着対応

`ApplySnap()` を方向ごとに分岐させる。`_direction` フィールドを追加して現在の吸着方向を保持する。

```csharp
private string _direction = "right"; // "right" | "left" | "top" | "bottom"

public void ApplySnap()
{
    var config = App.ConfigService.Current;
    var layout = config.Layout;
    var global = config.Global;
    _direction = global.SnapPosition;

    var (frameW, frameH) = WindowSizeCalculator.Calculate(global, layout);

    // モニター情報取得（マルチモニター § 9-3 参照）
    var monitor = GetTargetMonitor(global.SnapMonitor);
    double screenLeft = monitor.Left;
    double screenTop  = monitor.Top;
    double screenW    = monitor.Width;
    double screenH    = monitor.Height;

    switch (_direction)
    {
        case "right":
            _window.Width  = layout.HandleShortSide + layout.HandleFrameMargin + frameW;
            _baseFrameH    = frameH;
            _normalLeft    = screenLeft + screenW - layout.ScreenEdgeDistance - frameW
                             - layout.HandleFrameMargin - layout.HandleShortSide;
            _storedLeft    = _normalLeft + frameW + layout.HandleFrameMargin;
            _window.Left   = _normalLeft;
            _window.Top    = screenTop + (screenH - _window.Height) / 2;
            break;

        case "left":
            _window.Width  = layout.HandleShortSide + layout.HandleFrameMargin + frameW;
            _baseFrameH    = frameH;
            // 左吸着：フレームが左側、ハンドルが右側（XAML の列順を逆にする必要あり）
            _normalLeft    = screenLeft + layout.ScreenEdgeDistance;
            _storedLeft    = _normalLeft - frameW - layout.HandleFrameMargin;
            _window.Left   = _normalLeft;
            _window.Top    = screenTop + (screenH - _window.Height) / 2;
            break;

        case "top":
            // TODO: 上吸着（Ph.10 以降で実装）
            break;

        case "bottom":
            // TODO: 下吸着（Ph.10 以降で実装）
            break;
    }

    UpdateWindowHeight();
}
```

> **実装注意（左吸着）**：右吸着の XAML では `列0 = ハンドル`、`列2 = フレーム` の順。左吸着では `列0 = フレーム`、`列2 = ハンドル` が自然だが、XAML を動的に切り替えるのは複雑。初期フェーズは「右吸着」「左吸着」の 2 方向のみ実装し、上下は TODO としても構わない。

#### 9-3. マルチモニター対応

Win32 `EnumDisplayMonitors` を使ってモニター情報を取得する。

```csharp
[DllImport("user32.dll")]
private static extern bool EnumDisplayMonitors(IntPtr hdc, IntPtr lprcClip,
    MonitorEnumProc lpfnEnum, IntPtr dwData);
private delegate bool MonitorEnumProc(IntPtr hMonitor, IntPtr hdcMonitor,
    ref RECT lprcMonitor, IntPtr dwData);

[DllImport("user32.dll", CharSet = CharSet.Auto)]
private static extern bool GetMonitorInfo(IntPtr hMonitor, ref MONITORINFO lpmi);

[StructLayout(LayoutKind.Sequential)]
private struct RECT { public int Left, Top, Right, Bottom; }

[StructLayout(LayoutKind.Sequential, CharSet = CharSet.Auto)]
private struct MONITORINFO
{
    public int    cbSize;
    public RECT   rcMonitor;
    public RECT   rcWork;
    public uint   dwFlags;
}

private record MonitorRect(double Left, double Top, double Width, double Height);

private MonitorRect GetTargetMonitor(int snapMonitor)
{
    var monitors = new List<RECT>();
    EnumDisplayMonitors(IntPtr.Zero, IntPtr.Zero, (hMon, _, ref rc, _) =>
    {
        monitors.Add(rc);
        return true;
    }, IntPtr.Zero);

    // snapMonitor は 1 始まり
    int idx = Math.Clamp(snapMonitor - 1, 0, monitors.Count - 1);
    var rc  = monitors[idx];

    // 物理ピクセル → 論理ピクセル変換
    double dpiX = GetDpiScaleX();
    double dpiY = GetDpiScaleY();
    return new MonitorRect(
        rc.Left   / dpiX,
        rc.Top    / dpiY,
        (rc.Right  - rc.Left) / dpiX,
        (rc.Bottom - rc.Top)  / dpiY);
}

private double GetDpiScaleX()
{
    var source = PresentationSource.FromVisual(_window);
    return source?.CompositionTarget.TransformToDevice.M11 ?? 1.0;
}
```

#### 9-4. ドラッグ方向の方向別切替

現在は `OnMouseMove` で Y 方向のみ動く。吸着方向が `"top"` / `"bottom"` のときは X 方向のみ動くよう分岐する。

```csharp
// OnMouseMove 内、ドラッグ中の計算部分
double dpiScaleX = GetDpiScaleX();
double dpiScaleY = GetDpiScaleY();
double currentX = _window.PointToScreen(e.GetPosition(_window)).X;
double currentY = _window.PointToScreen(e.GetPosition(_window)).Y;

if (_direction is "right" or "left")
{
    // 縦スライド
    double newTop  = _dragStartTop + (currentY - _dragStartMouseY) / dpiScaleY;
    _window.Top    = Math.Clamp(newTop, 0, SystemParameters.PrimaryScreenHeight - _window.Height);
}
else
{
    // 横スライド（top / bottom 吸着）
    double newLeft = _dragStartLeft + (currentX - _dragStartMouseX) / dpiScaleX;
    _window.Left   = Math.Clamp(newLeft, 0, SystemParameters.PrimaryScreenWidth - _window.Width);
}
```

`_dragStartLeft` / `_dragStartMouseX` フィールドを追加し、`OnMouseDown` でも X 座標を記録する。

---

### 10. TrayIconService（新規）

`Services/TrayIconService.cs` を新規作成する。

```csharp
using System.Windows;
using Hardcodet.Wpf.TaskbarNotification;
using AppLauncher.Models;
using AppLauncher.ViewModels;

namespace AppLauncher.Services;

public class TrayIconService : IDisposable
{
    private TaskbarIcon? _trayIcon;
    private LauncherWindow? _window;

    public void Attach(LauncherWindow window)
    {
        _window    = window;
        _trayIcon  = (TaskbarIcon)Application.Current.Resources["TrayIcon"];
        _trayIcon.TrayMouseDoubleClick += OnTrayDoubleClick;
    }

    private void OnTrayDoubleClick(object sender, RoutedEventArgs e)
    {
        var vm = App.LauncherViewModel;
        if (vm == null) return;

        // 収納中は先に展開
        if (vm.IsStored)
        {
            App.SnapService?.ForceExpand(() => PinToggle(vm));
            return;
        }
        PinToggle(vm);
    }

    private static void PinToggle(LauncherViewModel vm)
    {
        _window?.Activate();
        vm.IsPinned = !vm.IsPinned;
    }

    public void Dispose() => _trayIcon?.Dispose();
}
```

`App.xaml` に `TaskbarIcon` リソースを追加する。

```xml
<Application ...>
    <Application.Resources>
        <tb:TaskbarIcon x:Key="TrayIcon"
                        IconSource="Resources/Icons/app.ico"
                        ToolTipText="AppLauncher">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <!-- 吸着位置サブメニュー -->
                    <MenuItem Header="吸着位置の指定">
                        <MenuItem x:Name="SnapRight"  Header="右" Click="OnSnapRight"/>
                        <MenuItem x:Name="SnapLeft"   Header="左" Click="OnSnapLeft"/>
                        <MenuItem x:Name="SnapTop"    Header="上" Click="OnSnapTop"/>
                        <MenuItem x:Name="SnapBottom" Header="下" Click="OnSnapBottom"/>
                    </MenuItem>
                    <MenuItem Header="ピンモード切替" Click="OnTogglePin"/>
                    <Separator/>
                    <MenuItem Header="終了" Click="OnExit"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Application.Resources>
</Application>
```

コンテキストメニューのハンドラは `App.xaml.cs` に実装する。

```csharp
private void OnSnapRight (object s, RoutedEventArgs e) => ChangeSnap("right");
private void OnSnapLeft  (object s, RoutedEventArgs e) => ChangeSnap("left");
private void OnSnapTop   (object s, RoutedEventArgs e) => ChangeSnap("top");
private void OnSnapBottom(object s, RoutedEventArgs e) => ChangeSnap("bottom");

private void ChangeSnap(string direction)
{
    // 収納中は先に展開してから変更
    if (LauncherViewModel?.IsStored == true)
        SnapService?.ForceExpand(() => ApplyNewSnap(direction));
    else
        ApplyNewSnap(direction);
}

private static void ApplyNewSnap(string direction)
{
    App.ConfigService.Current.Global.SnapPosition = direction;
    App.ConfigService.Save();
    App.SnapService?.ApplySnap();
}

private void OnTogglePin(object s, RoutedEventArgs e)
{
    if (LauncherViewModel != null)
        LauncherViewModel.IsPinned = !LauncherViewModel.IsPinned;
}

private void OnExit(object s, RoutedEventArgs e)
{
    _trayIconService?.Dispose();
    Shutdown();
}
```

`SnapService` に `ForceExpand(Action? callback)` メソッドを追加する。

```csharp
public void ForceExpand(Action? callback = null)
{
    if (App.LauncherViewModel != null)
        App.LauncherViewModel.IsStored = false;
    AnimateLeft(_window.Left, _normalLeft, () =>
    {
        if (!IsMouseInWindowOrGap())
            StartStorageTimer();
        callback?.Invoke();
    });
}
```

---

### 11. App.xaml / App.xaml.cs の変更

#### 11-1. App.xaml.cs

`SnapService` と `TrayIconService` をアプリケーション全体で管理する。

```csharp
public static SnapService? SnapService { get; private set; }

private TrayIconService? _trayIconService;

protected override void OnStartup(StartupEventArgs e)
{
    base.OnStartup(e);
    ConfigService.Load();

    var vm     = new LauncherViewModel(ConfigService.Current);
    LauncherViewModel = vm;
    var window = new LauncherWindow();
    var snap   = new SnapService();
    SnapService = snap;

    snap.Attach(window);
    window.SetViewModel(vm);
    window.Show();
    snap.ApplySnap();

    _trayIconService = new TrayIconService();
    _trayIconService.Attach(window);

    MainWindow = window;
}
```

#### 11-2. Alt+F4 → 最小化（タスクトレイ常駐 OFF 時は終了）

```csharp
protected override void OnExit(ExitEventArgs e)
{
    _trayIconService?.Dispose();
    base.OnExit(e);
}
```

`LauncherWindow.xaml.cs` の `OnClosing` をオーバーライドする（または `App.xaml.cs` で `MainWindow.Closing` をフックする）。

```csharp
protected override void OnClosing(CancelEventArgs e)
{
    if (App.ConfigService.Current.Global.TaskTrayResident)
    {
        e.Cancel = true;   // ウィンドウを閉じずに最小化扱いにする
        WindowState = WindowState.Minimized;
        return;
    }
    base.OnClosing(e);
}
```

#### 11-3. ShutdownMode 変更

`App.xaml` の `ShutdownMode` を `OnExplicitShutdown` に変更し、メインウィンドウを閉じてもアプリが終了しないようにする。

```xml
<Application ... ShutdownMode="OnExplicitShutdown">
```

---

### 12. StartupRegistryHelper（新規、小ヘルパー）

`Helpers/StartupRegistryHelper.cs` を新規作成する。

```csharp
using Microsoft.Win32;
using System.Reflection;

namespace AppLauncher.Helpers;

public static class StartupRegistryHelper
{
    private const string RunKey = @"SOFTWARE\Microsoft\Windows\CurrentVersion\Run";
    private const string AppName = "AppLauncher";

    public static void Apply(bool enable)
    {
        using var key = Registry.CurrentUser.OpenSubKey(RunKey, true);
        if (key == null) return;

        if (enable)
        {
            string? exePath = Assembly.GetExecutingAssembly().Location
                .Replace(".dll", ".exe");   // 単一 exe 発行時は .exe が正パス
            if (exePath != null)
                key.SetValue(AppName, $"\"{exePath}\"");
        }
        else
        {
            key.DeleteValue(AppName, throwOnMissingValue: false);
        }
    }
}
```

---

### 13. Debug ビルド：初期タイル配置

`ConfigService.cs` の初期設定生成部分に、Debug ビルド時のタイルを追加する（§ 3.8 の 13 タイル定義に従う）。

```csharp
#if DEBUG
// ページ1 に動作確認用タイルを配置（アプリケーション機能定義.md § 3.8 参照）
pages[0].Tiles = new List<TileConfig>
{
    new() { Col=0, Row=0, Type="app",    Title="メモ帳",  Path="notepad.exe",                  ImagePosition="top"   },
    new() { Col=1, Row=0, Type="url",    Title="Google",  Path="https://www.google.com",       ImagePosition="bottom"},
    new() { Col=2, Row=0, Type="folder", Title="Desktop", Path="%USERPROFILE%\\Desktop",       ImagePosition="left"  },
    new() { Col=3, Row=0, Type="folder", Title="Docs",    Path="%USERPROFILE%\\Documents",     ImagePosition="right" },
    new() { Col=4, Row=0, Type="system", Title="CPU",     SystemInfo=new(){ Category="usage",  Device="cpu", DisplayFormat="circle", UpdateIntervalMs=1000 }},
    new() { Col=5, Row=0, Type="system", Title="Memory",  SystemInfo=new(){ Category="usage",  Device="memory", DisplayFormat="text", UpdateIntervalMs=1000 }},
    new() { Col=0, Row=1, ColSpan=2, RowSpan=2, Type="app",    Title="電卓", Path="calc.exe" },
    new() { Col=2, Row=1, ColSpan=2, RowSpan=1, Type="webview", Title="Web", Path="https://www.google.com" },
    new() { Col=4, Row=1, ColSpan=1, RowSpan=2, Type="system", Title="C:", SystemInfo=new(){ Category="storage", Target="C:", DisplayFormat="text", UpdateIntervalMs=1000 }},
    new() { Col=5, Row=1, Type="system", Title="OS",     SystemInfo=new(){ Category="os", DisplayFormat="text", UpdateIntervalMs=60000 }},
    new() { Col=2, Row=2, Type="app",    Title="D&D",    Path="notepad.exe", Args="{drop}" },
    new() { Col=3, Row=2, Type="app",    Title="メモ帳", Path="notepad.exe" },
    new() { Col=0, Row=3, Type="app",    Title="GIF",    ImagePath="<debug用GIFのパス>" },
};
#endif
```

---

## 完了条件

- [x] 通常 → 編集 → 設定 → 通常の 3 ループ遷移が正しく動作する（ピンモード・収納禁止が各モードで正しく制御される）
- [x] 設定モードで「詳細設定」仮想ページが表示される
- [x] 設定タイル「全体設定」のクリックで全体設定パネルが開く
- [x] 全体設定パネルで各項目を変更し OK を押すと設定が反映・保存される
- [x] キャンセルで設定モードに戻り変更は破棄される
- [ ] タイル配置数を縮小しようとした場合、配置済みタイルがある範囲より小さくなれない
- [x] タスクトレイにアイコンが表示される
- [x] タスクトレイ右クリックメニューから吸着方向変更・ピンモード切替・終了が動作する
- [x] タスクトレイアイコンをダブルクリックするとピンモードが ON になる（収納中は先に展開）
- [x] タスクトレイ常駐 ON の状態で Alt+F4 を押してもアプリが終了しない（最小化扱い）
- [x] 右クリックメニューの「終了」でアプリが終了する
- [ ] 全体設定で「Windows 起動時の自動起動」を ON にするとレジストリに登録される
- [x] `dotnet build` がエラーなく成功する

### バグ修正・追加実装の完了条件

- [x] 編集モードでページ編集ボタンを押すとページ編集パネルが表示される
- [x] ページ編集パネルで名前・背景色・ハンドル色・ピン枠色を変更して OK で反映される
- [x] ページ編集パネルでキャンセルすると変更は破棄されて編集モードへ戻る
- [x] 編集モードのボトムバーに全体設定ボタンは表示されない
- [x] ウィンドウをドラッグ移動した後に再起動すると前回の位置で起動する（左右吸着は上下位置、上下吸着は左右位置）
- [x] 下端吸着時にタスクバーの上端を画面端として扱う（タスクバーに被らない）
- [x] 吸着方向変更後に自動的に収納判定が行われる（マウスが範囲外であれば収納される）
- [x] 左右吸着の収納時にハンドルが画面端に接する位置まで移動する
- [x] 上下吸着の隙間（`screenEdgeDistance` 分）にカーソルがある間はウィンドウが展開状態を維持する
- [x] ページ編集モード中は収納されない
- [x] `system` タイルで画像 / アイコンが表示される（top / bottom / left / right / center の5方向対応）
- [x] 画像表示位置で「中心」が選択でき、画像の上にテキストが重なって表示される
- [x] 画像とテキストが重なる場合、テキストが常に画像の前面に描画される

---

## バグ修正・追加実装（Ph.10 実装後）

### A. GlobalConfig への位置保存フィールド追加

`Models/Config/GlobalConfig.cs` に以下を追加する。`double.NaN` は `System.Text.Json` がシリアライズできないため `double?` で `null` をデフォルトとする。

```csharp
public double? SavedWindowTop  { get; set; } = null;
public double? SavedWindowLeft { get; set; } = null;
```

---

### B. PageEditViewModel（新規）

`ViewModels/PageEditViewModel.cs` を新規作成する。

```csharp
using CommunityToolkit.Mvvm.ComponentModel;
using AppLauncher.Models;
using AppLauncher.Models.Config;

namespace AppLauncher.ViewModels;

public partial class PageEditViewModel : ObservableObject
{
    [ObservableProperty] private string _name;
    [ObservableProperty] private string _backgroundColor;
    [ObservableProperty] private int    _backgroundOpacity;
    [ObservableProperty] private string _handleColor;
    [ObservableProperty] private int    _handleOpacity;
    [ObservableProperty] private string _pinFrameColor;
    [ObservableProperty] private int    _pinFrameOpacity;

    public IReadOnlyList<string> ColorOptions { get; } = [.. ColorPalette.All.Keys];

    public PageEditViewModel(PageViewModel source)
    {
        _name             = source.Name;
        _backgroundColor  = source.BackgroundColor;
        _backgroundOpacity = source.BackgroundOpacity;
        _handleColor      = source.HandleColor;
        _handleOpacity    = source.HandleOpacity;
        _pinFrameColor    = source.PinFrameColor;
        _pinFrameOpacity  = source.PinFrameOpacity;
    }

    public PageConfig ToConfig(IReadOnlyList<TileViewModel> tiles) => new()
    {
        Name             = Name,
        BackgroundColor  = BackgroundColor,
        BackgroundOpacity = BackgroundOpacity,
        HandleColor      = HandleColor,
        HandleOpacity    = HandleOpacity,
        PinFrameColor    = PinFrameColor,
        PinFrameOpacity  = PinFrameOpacity,
        Tiles            = tiles.Select(t => t.ToConfig()).ToList(),
    };
}
```

---

### C. PageEditView（新規）

`Views/Controls/PageEditView.xaml` を新規作成する。TileEditControl / GlobalSettingsView と同じフレーム全面オーバーレイ方式。

- `Grid.RowSpan="7"`・`Panel.ZIndex="50"` で最前面に表示
- `DataContext="{Binding PageEditVm}"` で `LauncherViewModel.PageEditVm` にバインド
- フォーム内容：名前テキストボックス、背景色プルダウン、背景透過率スライダー、ハンドル色、ハンドル透過率、ピン枠色、ピン枠透過率
- OK / キャンセルボタン（`ApplyUiElementColor()` で背景色を自動計算）

`Views/Controls/PageEditView.xaml.cs`:

```csharp
public partial class PageEditView : UserControl
{
    public PageEditView()
    {
        InitializeComponent();
        OkButton.MouseLeftButtonUp     += (_, _) => App.LauncherViewModel?.ConfirmPageEditCommand.Execute(null);
        CancelButton.MouseLeftButtonUp += (_, _) => App.LauncherViewModel?.ClosePageEditCommand.Execute(null);
        Loaded += (_, _) => ApplyUiElementColor();
    }

    public void ApplyUiElementColor()
    {
        var page = App.LauncherViewModel?.CurrentPage;
        if (page == null) return;
        var colors = ColorHelper.ComputeUiElementColor(page.BackgroundColor, page.BackgroundOpacity);
        Background          = colors.Background;
        OkButton.Background     = colors.Accent;
        CancelButton.Background = colors.Dim;
    }
}
```

---

### D. LauncherViewModel へのページ編集コマンド追加

```csharp
[ObservableProperty]
private PageEditViewModel? _pageEditVm;

[RelayCommand]
private void OpenPageEdit()
{
    PageEditVm = new PageEditViewModel(CurrentPage);
    Mode = AppMode.PageEdit;
}

[RelayCommand]
private void ConfirmPageEdit()
{
    if (PageEditVm == null) return;
    int idx = CurrentPageIndex;
    var newConfig = PageEditVm.ToConfig(Pages[idx].Tiles);
    PageEditVm = null;
    Pages[idx] = new PageViewModel(newConfig);
    OnPropertyChanged(nameof(CurrentPage));
    OnPropertyChanged(nameof(DisplayPage));
    SyncPagesToConfig();
    App.ConfigService.Save();
    Mode = AppMode.Edit;
}

[RelayCommand]
private void ClosePageEdit()
{
    PageEditVm = null;
    Mode = AppMode.Edit;
}
```

また `OpenGlobalSettings()` / `ConfirmGlobalSettings()` / `CloseGlobalSettings()` は、`AppMode.Settings` へ戻る前に設定モード前のモードを保持するフィールド `_modeBeforeGlobalSettings` を使って実装する。

---

### E. LauncherWindow の変更（PageEdit パネル追加）

`LauncherWindow.xaml` に TileEditPanel / GlobalSettingsPanel と同じ形式で追加：

```xml
<controls:PageEditView x:Name="PageEditPanel"
                       Grid.RowSpan="7"
                       Visibility="Collapsed"
                       Panel.ZIndex="50"/>
```

`LauncherWindow.xaml.cs` の `SetViewModel()` PropertyChanged ハンドラに追加：

```csharp
bool isPageEdit = vm.Mode == AppMode.PageEdit;
PageEditPanel.Visibility = isPageEdit ? Visibility.Visible : Visibility.Collapsed;
if (isPageEdit)
    PageEditPanel.ApplyUiElementColor();
```

---

### F. BottomBarControl の変更（編集モードは PageEdit ボタンのみ）

`Rebuild()` 内で全体設定ボタンを編集モードに表示しない。

```csharp
// ページ編集ボタン：編集モードのみ表示
PageEditButton.Visibility = isEdit ? Visibility.Visible : Visibility.Collapsed;
if (isEdit) SetPageBtn(PageEditButton, "🗒", true);

// 全体設定ボタン：常に非表示（全体設定は設定モードのタイルから遷移）
GlobalSettingsButton.Visibility = Visibility.Collapsed;
```

---

### G. SnapService のバグ修正・拡張

#### G-1. MonitorRect に WorkBottom を追加（下端タスクバー対応）

```csharp
private record MonitorRect(double Left, double Top, double Width, double Height, double WorkBottom);
```

`GetTargetMonitor()` で `GetMonitorInfo` を呼び出して `rcWork.Bottom` を取得し `WorkBottom` に格納する。下端吸着では `monitor.WorkBottom` を画面端として使用する。

```csharp
case "bottom":
    _normalTop = monitor.WorkBottom - layout.ScreenEdgeDistance - frameH
                 - layout.HandleFrameMargin - layout.HandleShortSide;
    _storedTop = monitor.WorkBottom - layout.HandleShortSide;
```

#### G-2. 収納位置修正（ハンドルが画面端に接する）

左右吸着で収納後にハンドルが `screenEdgeDistance` 分だけ内側に残る問題を修正。

```csharp
case "right":
    _storedLeft = screenRight - layout.HandleShortSide;   // ハンドル左端 = 画面右端 - ハンドル幅
case "left":
    _storedLeft = screenLeft - frameW - layout.HandleFrameMargin;  // フレーム右端 = 画面左端
```

#### G-3. IsMouseInWindowOrGap の上下向き修正

上端吸着の隙間は「フレームより上（画面端側）」、下端吸着の隙間は「フレームより下（ワークエリア下端側）」にある。

```csharp
bool inY = _direction switch
{
    "top"    => pos.Y >= -gap && pos.Y <= _window.Height,      // gap は上側（負の方向）
    "bottom" => pos.Y >= 0   && pos.Y <= _window.Height + gap, // gap は下側（正の方向）
    _        => pos.Y >= 0   && pos.Y <= _window.Height,
};
```

#### G-4. ApplySnap 後の自動収納チェック

吸着方向変更時にウィンドウが移動しても `MouseLeave` は発火しない。`Dispatcher.BeginInvoke` で遅延実行して収納判定する。

```csharp
// ApplySnap() の末尾
_window.Dispatcher.BeginInvoke(System.Windows.Threading.DispatcherPriority.Loaded, () =>
{
    var vm = App.LauncherViewModel;
    if (vm == null || vm.IsPinned || vm.IsStored || _isAnimating) return;
    if (vm.Mode is AppMode.Settings or AppMode.GlobalSettings or AppMode.PageEdit) return;
    if (!IsMouseInWindowOrGap())
        StartStorageTimer();
});
```

#### G-5. SavePosition（ドラッグ後の位置保存）

```csharp
private void OnMouseUp(object sender, MouseButtonEventArgs e)
{
    bool wasDragging = _isDragging;
    if (_isDragging) _window.ReleaseMouseCapture();
    _isDragging = false;
    if (wasDragging) SavePosition();
}

private void SavePosition()
{
    var global = App.ConfigService.Current.Global;
    if (IsVerticalSnap)                    // top / bottom
        global.SavedWindowLeft = _window.Left;
    else                                   // left / right
        global.SavedWindowTop  = _window.Top;
    App.ConfigService.Save();
}
```

`ApplySnap()` の起動位置決定では `configSavedTop.HasValue` / `configSavedLeft.HasValue` で保存値の有無を判定する。

#### G-6. PageEdit モードを収納禁止・高さ拡張に追加

```csharp
// OnWindowMouseLeave・StorageTimerTick
if (vm.Mode is AppMode.Settings or AppMode.GlobalSettings or AppMode.PageEdit) return;

// UpdateWindowHeight
bool isExpanded = App.LauncherViewModel?.Mode is AppMode.Edit or AppMode.TileEdit or AppMode.PageEdit;
```

---

### H. タイル画像機能の拡張

#### H-1. system タイルへの画像表示対応

`SystemTileControl.xaml` の構造を変更し、`TileControl` と同様の `LayoutGrid` + `TileImage` + `ContentGrid` 構成を採用する。

**XAML 構造：**

```xml
<Grid Background="Transparent">
    <Grid x:Name="LayoutGrid">
        <!-- RowDef[0]=*, RowDef[1]=0 / ColDef[0]=*, ColDef[1]=0 -->
        <Image x:Name="TileImage" Grid.Row="0" Grid.Column="0"
               Stretch="Uniform" Margin="8" Visibility="Collapsed"/>
        <!-- システム情報コンテンツ（画像より前面） -->
        <Grid x:Name="ContentGrid" Grid.Row="0" Grid.Column="0" Panel.ZIndex="1">
            <StackPanel x:Name="TextPanel" .../>
            <Canvas x:Name="CirclePanel" .../>
        </Grid>
    </Grid>
    <Grid x:Name="EditOverlay" Visibility="Collapsed">...</Grid>
</Grid>
```

**`SystemTileControl.xaml.cs` への追加：**

- フィールド：`_imagePath`, `_gifDecoder`, `_gifFrameIndex`, `_gifTimer`
- `Apply()` 末尾に `SetupImageAndLayout(tile)` を追加
- `SetupImageAndLayout()`：画像の有無・透過設定を適用し `ArrangeImageAndContent()` を呼ぶ
- `ArrangeImageAndContent(string position, bool hasImage)`：`TileControl` の `ArrangeImageAndText()` と同等ロジック（`TitleText` の代わりに `ContentGrid` を操作）
- `StartGif()` / `StopGif()`：`TileControl` と同じ GIF 再生ロジック
- `OnLoaded()` に画像読み込み処理（`.gif` は `StartGif()`、それ以外は `BitmapImage`）を追加
- `OnUnloaded()` に `StopGif()` を追加

`using System.IO;` および `using System.Windows.Media.Imaging;` を追加する。

#### H-2. 画像表示位置「中心」追加

`"center"` 指定時、画像とテキスト/コンテンツが同一セルを占有して重なる。テキストは `Panel.ZIndex=1` で前面に描画される。

**`TileControl.xaml.cs`** `ArrangeImageAndText()` への追加：

```csharp
case "center":
    // 画像とテキストを同一領域に重ねる。TitleText の Panel.ZIndex=1 でテキストが前面
    Grid.SetRowSpan(TileImage, 2);
    Grid.SetColumnSpan(TileImage, 2);
    Grid.SetRowSpan(TitleText, 2);
    Grid.SetColumnSpan(TitleText, 2);
    Grid.SetRow(TileImage, 0);    Grid.SetColumn(TileImage, 0);
    Grid.SetRow(TitleText, 0);    Grid.SetColumn(TitleText, 0);
    break;
```

**`SystemTileControl.xaml.cs`** `ArrangeImageAndContent()` への追加：

```csharp
case "center":
    Grid.SetRowSpan(TileImage, 2);
    Grid.SetColumnSpan(TileImage, 2);
    Grid.SetRowSpan(ContentGrid, 2);
    Grid.SetColumnSpan(ContentGrid, 2);
    Grid.SetRow(TileImage, 0);    Grid.SetColumn(TileImage, 0);
    Grid.SetRow(ContentGrid, 0);  Grid.SetColumn(ContentGrid, 0);
    break;
```

**`TileEditControl.xaml.cs`** `ImagePositions` への追加：

```csharp
new("center", "中心"),
```

#### H-3. 文字の前面表示（Panel.ZIndex）

画像と文字が重なる位置に配置された場合、テキストが常に画像の前面に表示されるよう XAML で明示的に `Panel.ZIndex` を設定する。

| ファイル | 要素 | 変更内容 |
|---|---|---|
| `TileControl.xaml` | `TitleText` | `Panel.ZIndex="1"` を追加 |
| `SystemTileControl.xaml` | `ContentGrid` | `Panel.ZIndex="1"` を設定（XAML 上で明示） |

---

## 参照定義書

- `definition/function/アプリケーション機能定義.md § 2.4` — 設定モードの遷移・仮想ページ仕様
- `definition/function/アプリケーション機能定義.md § 2.7` — 全体設定モードの設定項目一覧
- `definition/function/アプリケーション機能定義.md § 3.1` — 画面吸着・4 方向・マルチモニター仕様
- `definition/function/アプリケーション機能定義.md § 3.7` — タスクトレイ・コンテキストメニュー仕様
- `definition/function/アプリケーション機能定義.md § 3.8` — Debug 初期タイル配置
