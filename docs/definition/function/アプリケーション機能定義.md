# アプリケーション機能定義書

## 目次

1. [概要](#1-概要)
2. [アプリケーション状態定義](#2-アプリケーション状態定義)
   - 2.1 [通常モード](#21-通常モード)
   - 2.2 [通常モード（ピンモード ON）](#22-通常モードピンモード-on)
   - 2.3 [編集モード](#23-編集モード)
   - 2.4 [設定モード](#24-設定モード)
   - 2.5 [タイル編集モード](#25-タイル編集モード)
   - 2.6 [ページ編集モード](#26-ページ編集モード)
   - 2.7 [全体設定モード](#27-全体設定モード)
   - 2.8 [収納モード](#28-収納モード)
3. [機能仕様](#3-機能仕様)
   - 3.1 [画面吸着](#31-画面吸着)
   - 3.2 [タイル](#32-タイル)
   - 3.3 [タイル操作（編集モード）](#33-タイル操作編集モード)
   - 3.4 [タイルの種類](#34-タイルの種類)
   - 3.5 [タイル詳細設定](#35-タイル詳細設定)
   - 3.6 [カラーパレット](#36-カラーパレット)
   - 3.7 [右クリックメニュー](#37-右クリックメニュー)
   - 3.8 [初回起動時の初期設定](#38-初回起動時の初期設定)
4. [共通仕様](#4-共通仕様)
   - 4.1 [透過率](#41-透過率)

---

## 1. 概要

このドキュメントは、アプリランチャーの実装機能を定義します。

**実装技術**：WPF（C# / .NET 8）。詳細は [`../implementation/技術スタック.md`](../implementation/技術スタック.md) を参照。

---

## 2. アプリケーション状態定義

アプリケーションは以下に示す状態を遷移しながら動作します。

### 2.1 通常モード

アプリランチャーとして動作する基本状態です。

**遷移条件**
- アプリ起動時に初期状態としてこのモードで起動します。
- 設定モードでモード切替ボタンを押すと、このモードへ戻ります。
- 収納モード時にハンドル部分へマウスオーバーすると、このモードへ遷移します。

- ランチャーのドラッグによる位置移動は、通常モード（ピンモード ON/OFF 問わず）で行えます。
- 収納モードへ移行できるのは、このモードのみです。
- 吸着先の画面端とランチャー本体の間に生じる隙間にカーソルがある場合は、本体にフォーカスがあるものとして扱い、自動収納を抑制します。（収納 → 展開の無限ループを防止するための措置）

### 2.2 通常モード（ピンモード ON）

収納モードへの移行を禁止するモードです。

**遷移条件**
- 通常モードで、アプリのタイル・ボタン・ボトムバー外の領域をダブルクリックするとピンモードが ON になります。
- 右クリックメニューからピンモードを選択することでも切り替えられます。
- ピンモード ON の状態で同じ操作を行うと、ピンモードが OFF になり通常モードへ戻ります。
- 編集モードから通常モードへ戻る際、ユーザーが事前にピンモードを設定していた場合はピンモード ON の状態が維持されます。

- ドラッグによる位置移動は通常モード（ピンモード OFF）と同様に可能です。
- ユーザーが任意に切り替えを行えます。
- 自動収納されると困る場面での活用を想定しています。

### 2.3 編集モード

タイル・ページ・全体設定の編集が行える状態です。

**遷移条件**
- 通常モード（ピンモード ON/OFF 問わず）でモード切替ボタンを押すとこのモードへ遷移します。
- 編集モードで再びモード切替ボタンを押すと設定モードへ遷移します。

- 編集モードに移行すると、自動的にピンモードが ON になります。このとき、フレームとハンドルの実線がピンモード配色に切り替わります。
- 以下の操作が可能になります。
  - タイルの追加・削除・移動・編集
  - ページの追加・削除・移動・編集
  - ページ名称エリアをクリックすることでページ名を直接入力・変更可能
- ページを削除した場合、削除されたページの左隣のページへ移動します（削除されたページが最も左のページだった場合は、右隣のページへ移動します）。

**ページ名のインライン編集**

- 文字数の上限なし。
- キャンセル操作なし（入力を中断する手段は提供しない）。
- ページを切り替える（ページインジケーターのクリック等）場合、または編集モードを抜ける（タイル編集モード・ページ編集モード・設定モードへ遷移する）際に、入力中の文字列でページ名を確定する。

### 2.4 設定モード

各設定画面へ遷移するためのハブとなる状態です。タイルグリッドの代わりに「詳細設定」専用の仮想ページが表示されます。

**遷移条件**
- 編集モードでモード切替ボタンを押すとこのモードへ遷移します。
- 設定モードで再びモード切替ボタンを押すと通常モードへ戻ります。このとき、ユーザーが手動でピンモードを設定していた場合を除きピンモードは解除されます。
- 設定タイルをクリックすると対応する設定画面へ遷移します。

- 設定モードに移行すると、自動的にピンモードが ON になります（編集モードと同様）。
- 収納モードへは移行しません。
- 以下の操作が可能になります。
  - 設定タイルのクリックによる各設定画面への遷移

**仮想ページ仕様**

| 項目 | 仕様 |
|---|---|
| ページタイトル | 「詳細設定」（固定） |
| タイル外観 | gray（`#8C9BAB`）・透過率 0 %（完全不透明） |
| タイル配置 | グリッド座標 (0,0) を起点に右方向へ順に配置 |
| ページ数 | 設定タイル数 ÷ 1 ページあたりの表示可能タイル数（端数切り上げ）で自動決定 |
| ページ名のインライン編集 | 不可 |

**ボトムバー（設定モード時）**
- 行 1：ページインジケーター（中央）＋ モード切替ボタン（右寄せ）のみ
- ページ管理コントロール（行 2）は表示しない
- ページ編集ボタンは表示しない

**現在の設定タイル一覧**

| タイル | 遷移先 |
|---|---|
| 全体設定 | 全体設定モード |

---

### 2.5 タイル編集モード

タイルの詳細設定を操作できる状態です。

**遷移条件**
- 編集モードで配置済みタイル上の編集ボタンを押すとこのモードへ遷移します。
- 「OK」ボタンを押すと設定が保存・反映され、編集モードへ戻ります。
- 「キャンセル」ボタンを押すと設定を破棄し、編集モードへ戻ります。

- タイルの見た目（色・アイコンなど）や動作内容の細かい設定が可能です。

### 2.6 ページ編集モード

ページ個別の設定を操作できる状態です。

**遷移条件**
- 編集モード時に表示されるページ編集ボタンを押すとこのモードへ遷移します。
- 「OK」ボタンを押すと設定が保存・反映され、編集モードへ戻ります。
- 「キャンセル」ボタンを押すと設定を破棄し、編集モードへ戻ります。

- ランチャーは複数のページで構成されており、各ページにタイルを配置できます。
- ページの見た目はページごとに独立して設定・管理されます。

**設定項目**

| 設定項目 | 説明 |
|---|---|
| ページ背景色 | ページ背景の色 |
| ページ背景の透過率 | ページ背景の透明度（[共通仕様参照](#4-共通仕様)） |
| ハンドル色 | ハンドルの色 |
| ハンドルの透過率 | ハンドルの透明度（[共通仕様参照](#4-共通仕様)） |
| ピンモード枠色 | ピンモード ON 時のフレーム・ハンドル実線色 |
| ピンモード枠透過率 | ピンモード ON 時の実線の透明度（[共通仕様参照](#4-共通仕様)） |

### 2.7 全体設定モード

アプリ全体に関わる設定を行う状態です。

**遷移条件**
- 設定モードで「全体設定」タイルをクリックするとこのモードへ遷移します。
- 「OK」ボタンを押すと設定が保存・反映され、設定モードへ戻ります。
- 「キャンセル」ボタンを押すと設定を破棄し、設定モードへ戻ります。

| 設定項目 | 説明 |
|---|---|
| タスクトレイ常駐 | ON/OFF の切り替え |
| Windows 起動時の自動起動 | ON/OFF の切り替え |
| タイル配置数（縦・横） | 縦・横それぞれ独立して設定可能（各 4 ～ 16）。この値によりアプリ本体のサイズが変動する |
| タイル間マージン | タイル同士の間隔（0 ～ 30 px） |
| ページ名フォントサイズ | ページ名テキストのフォントサイズ（[共通仕様参照](#4-共通仕様)） |
| 吸着位置 | 画面端への吸着方向（上・右・下・左） |
| 吸着先モニター | 吸着するモニターを番号で選択（1 = 主モニター、2 以降 = サブモニター）。接続中のモニター数に応じた選択肢を表示する |
| 収納待ち時間 | マウスオーバーがない状態が続いてから収納モードへ移行するまでの時間（0 ～ 3000 ms、デフォルト：1000 ms）。0 ms の場合はマウスオーバーが外れた瞬間に即座に収納される |

> **タイル配置数の縮小制限**：縦・横いずれかを縮小する場合、配置済みタイルが占有する最大座標を下回る方向への縮小はできません。

### 2.8 収納モード

ランチャー本体が画面端へ収納され、ハンドル部分のみが画面上に表示されている状態です。

**遷移条件**
- ピンモードが OFF の状態で、アプリへのマウスオーバーがない状態が収納待ち時間（デフォルト：1000 ms）続くと、このモードへ移行します。収納待ち時間が 0 ms の場合は、マウスオーバーが外れた瞬間に即座に移行します。
- 収納中にハンドル部分へマウスオーバーすると収納が解除され、通常モードへ戻ります。

**動作仕様**
- 収納先は吸着方向に応じて変わります。（例：右端吸着中であれば、右画面端へ収納）
- 収納中はハンドル部分のみが画面内に表示されます。
- 通常モード → 収納モード、および収納モード → 通常モードの移行時、ランチャー本体が画面端へ入り込む・出てくるアニメーションが再生されます。
- 収納・収納解除のアニメーションはそれぞれ 1 秒で完了します。
- 画面下端に吸着している場合、タスクバーの上端を画面端として扱い、タスクバーの下側へ潜り込む形で収納されます。

---

## 3. 機能仕様

### 3.1 画面吸着

- アプリは画面端から一定距離を保った位置に吸着し、それ以上離れる方向には移動できません。
- 移動可能な方向は、吸着面に平行な方向のみです。（右端吸着の場合、上下方向のみスライド可能）
- アプリ上の任意の箇所をマウスドラッグすることで移動できます。ScrollViewer 内のコントロール（タイル編集パネル等）の上でも正しくドラッグ開始できます。
- ドラッグ中は、マウスの移動量に比例した自然な速度で追従します。（感覚から乖離した移動速度は禁止）
- 吸着方向は**上・右・下・左**の 4 方向です。
- 初回起動時は画面右端の中央に吸着した状態で起動します。
- 全体設定で吸着位置を変更した場合、変更は直後にアニメーションなしで反映されます（瞬間移動）。
- 画面端とハンドル外端の間には一定の距離を設けます（デフォルト：20 px）。
- ランチャーは常に他のウィンドウの最前面に表示されます（alwaysOnTop）。
- マルチモニター環境では、サブモニターの画面端にも吸着できます。吸着先のモニターは**全体設定モード**で番号により選択します（1 = 主モニター、2 以降 = サブモニター）。接続中のモニター数が選択肢の上限となります。

### 3.2 タイル

- タイル配置可能領域内で、タイルの作成・移動・サイズ変更が行えます。
- タイルサイズは **1×1（最小）〜 4×4（最大）** の範囲で設定できます。
- 全体設定モードで指定した縦横のタイル配置数に応じて、アプリ本体のサイズが変動します。
- タイルには以下の表示設定が可能です。
  - 背景色・透明度の設定
  - 画像・アイコンの表示
- タイル上にマウスカーソルを乗せると、ホバーアニメーションが再生されます（通常モードのみ。底面を基準に 1.13 倍に拡大）。編集モード・設定モード中はホバーアニメーションは発生しません。
- ホバーアニメーションで拡大したタイルは、隣接する他のタイルの上に重なって表示されます（他のタイルは移動しません）。
- 画像ファイルとして GIF が設定されている場合、GIF アニメーションをループ再生します。非表示ページに配置されたタイルの GIF アニメーションは一時停止し、ページが再表示された際に再開します。
- ページの切り替えはアニメーションなしで即座に切り替わります。

### 3.3 タイル操作（編集モード）

**タイルの新規作成（クリック）**

1. タイル未配置領域にカーソルを移動すると、1×1 サイズの配置プレビュー枠が表示されます。
2. プレビュー枠内でクリックすると、以下のデフォルト設定で新規タイルが作成されます。

| 設定項目 | デフォルト値 |
|---|---|
| 種類 | `app` |
| パス / 引数 | 未設定 |
| 背景色 | blue（`#4A90D9`） |
| 透過率 | 20 % |
| フォントサイズ | 16 pt |
| フォントカラー | white（`#F0F0F0`） |
| 画像 / アイコン表示位置 | 上 |

**タイルの新規作成（ドラッグ＆ドロップ）**

- 対応ファイル形式：実行ファイル（.exe）、スクリプトファイル、HTML ファイル、ショートカット（.lnk）、画像ファイル
- タイル未配置領域へドラッグすると配置プレビュー枠が表示され、ドロップするとファイルに応じた設定が自動登録されたタイルが作成されます。
- 見た目のデフォルト値（背景色・透過率・フォントサイズ・フォントカラー・画像表示位置）はクリック作成時と同じ値を適用します。

**ドロップファイルとタイル種別のマッピング**

| ドロップしたファイル種別 | 作成されるタイル種別 | 自動設定される内容 |
|---|---|---|
| 実行ファイル（.exe） | `app` | パスに .exe のパスを設定 |
| ショートカット（.lnk） | `app` | パスにリンク先の実行ファイルパスを設定 |
| スクリプトファイル（.bat / .ps1 / .vbs 等） | `app` | パスにスクリプトファイルのパスを設定 |
| HTML ファイル（.html / .htm） | `app` | パスに HTML ファイルのパスを設定（ブラウザで起動） |
| 画像ファイル（.png / .jpg / .gif 等） | `app` | 画像 / アイコンパスに画像ファイルのパスを設定。画像 / アイコン表示位置を「上」に設定 |

**配置済みタイルの操作**

配置済みタイル上でマウスオーバーすると、以下のコントロールが表示されます。

| コントロール | 動作 |
|---|---|
| 編集ボタン | タイル編集モードを開く |
| 削除ボタン | 確認ダイアログを表示する |
| コピーボタン | タイルのコピーモードを開始する（後述） |
| サイズ変更領域 | 右下コーナーをドラッグして 4×4 以内の任意サイズへ変形。変形中は配置プレビュー枠が表示され、他のタイルと重なる位置には配置不可 |

**タイルのコピー**

コピーボタン（⊕）を押すとコピーモードに移行します。

1. コピーモード中はカーソルがコピーモード専用に変わります。
2. タイル未配置領域をクリックすると、元タイルの設定をそのままコピーした **1×1** の新規タイルを配置します（サイズは常に 1×1 になります）。
3. 右クリックまたは Escape でコピーモードをキャンセルします。
4. コピーモード中も配置プレビュー枠が表示され、他のタイルと重なる位置には配置不可です。

**タイル削除の確認ダイアログ**

- ランチャーフレームの中央に確認ダイアログを表示する。
- ダイアログ背後はランチャーの直前の画面が透けて見えるオーバーレイを表示する。
- ダイアログはデフォルトカラー（gray：`#8C9BAB`）で描画する。
- ダイアログサイズ：300 × 140 px
- 表示内容：「タイルを削除しますがよろしいですか？」
- ボタン：「OK」（削除実行）・「キャンセル」（削除中止）

**タイルの移動**

- サイズ変更領域・ボタン以外の箇所をドラッグすると、タイルを移動できます。
- ドラッグ中はカーソル位置にタイルサイズの配置プレビュー枠が表示されます。
- 他のタイルが占有している領域には移動不可です。
- 同じサイズのタイル上でドロップすると、2 つのタイルの位置が入れ替わります。入れ替え元・入れ替え先のどちらかが D&D 専用タイルの場合も入れ替えを行います。
- 異なるサイズのタイル上でドロップした場合は何も行いません（移動キャンセル）。
- ドラッグ中は、マウスの移動量に比例した自然な速度で追従します。
- **ページをまたぐ移動**：ドラッグ中にページインジケーターのホバーでページを切り替えた後、そのページの空き領域へドロップすることで別ページへタイルを移動できます。

**ドラッグ中のページ切り替え**

編集モード中にタイルをドラッグしながらボトムバーのページインジケーターへマウスオーバーすると、700 ms 後に自動的にそのページへ切り替わります。これにより、ドラッグしたまま別ページへ移動して任意の位置にドロップできます。

- ホバー中のインジケーターはハイライト（白色半透明）で表示されます。
- マウスがインジケーターから離れた場合はタイマーがキャンセルされます。

### 3.4 タイルの種類

| 種別 | 説明 |
|---|---|
| `app` | 指定したアプリケーションを起動する |
| `url` | 指定した URL をブラウザで開く |
| `folder` | 指定したパスのフォルダを開く |
| `webview` | 指定したパスまたは URL の内容をタイル内に表示する |
| `system` | システム情報を取得してタイル内に表示する |

#### 種別ごとのクリック動作

**`app`**
- パスに指定されたアプリケーションを起動する。
- 引数が設定されている場合はコマンドライン引数として渡す。
- 作業フォルダが設定されている場合はそのフォルダをカレントディレクトリとして起動する。
- 引数フィールドに `{drop}` 構文が含まれる場合は D&D 専用タイルとして動作し、クリックによる起動は行わない（[詳細は 3.5 D&D 構文を参照](#35-タイル詳細設定)）。

**`url`**
- 指定した URL をデフォルトブラウザで開く。

**`folder`**
- 指定したパスのフォルダをエクスプローラーで開く。

**`webview`**
- 指定した URL またはローカルの HTML ファイルをタイル内に表示する。
- クリック時の動作は表示している HTML 側の実装に委ねる。
- 通常モードにおいて、タイル内コンテンツへのマウス操作（クリック・スクロール・テキスト入力等）が可能。
- ホバーアニメーション時はタイル内コンテンツごと 1.13 倍に拡大される。
- 非表示ページに配置された場合も、コンテンツのロードは継続されます。

**`system`**
- システム情報を取得してタイル内に表示する。
- クリック時の動作はタイル詳細設定のシステム情報セクション内「クリック動作」の設定内容に従う。
- 他の種別と同様に、見た目セクション（§3.5）で画像 / アイコンの表示が可能です。システム情報（テキスト・円形グラフ）は常に画像の前面に描画されます。

### 3.5 タイル詳細設定

#### 基本セクション

| 設定項目 | 説明 |
|---|---|
| タイトル | タイルに表示するラベル |
| 種類 | タイルの種別（[3.4 参照](#34-タイルの種類)） |

#### システム情報セクション（種類が `system` の場合のみ表示）

情報カテゴリの選択内容によって、各設定項目の有効/無効が変わります。

| 設定項目 | 説明 |
|---|---|
| 情報カテゴリ | 表示するシステム情報の分類 |
| デバイス種別 | 対象デバイスの種別（カテゴリによって選択可否が変わる） |
| 対象 | 情報取得の対象（カテゴリによって選択可否が変わる） |
| 表示形式 | 情報の表示スタイル（カテゴリによって選択肢が変わる） |
| メインカラー | テキスト・グラフの主要表示色 |
| アクセント | 使用率が基準値を超えた際のグラフ色（カテゴリによって選択可否が変わる） |
| 背景アクセント | 使用率が基準値を超えた際のタイル背景色（カテゴリによって選択可否が変わる） |
| 基準値 | アクセント適用のしきい値（カテゴリによって選択可否が変わる） |
| クリック動作 | タイルクリック時のアクション。現時点では「表示更新」のみ（データを再取得し描画を更新する） |
| 更新間隔 | 情報を自動更新する間隔（ms 単位）。最小値：100 ms、最大値：3,600,000 ms（60 分）、デフォルト：60,000 ms（1 分） |

非表示ページに配置された `system` タイルの自動更新タイマーは一時停止します。ページが再表示された際は即時データ取得を行い、その後タイマーを再開します。データ取得はバックグラウンドスレッドで非同期に行われるため、ページ切り替え時に UI がブロックされません。

##### 情報カテゴリ：OS情報

OS の名称・バージョンを表示します。

| 設定項目 | 内容 |
|---|---|
| デバイス種別 | 選択不可 |
| 対象 | 選択不可 |
| 表示形式 | 「テキスト表示」のみ |
| メインカラー | 名称・バージョンのテキスト色 |
| アクセント | 選択不可 |
| 背景アクセント | 選択不可 |
| 基準値 | 選択不可 |
| クリック動作 | 「表示更新」のみ |

##### 情報カテゴリ：ストレージ

ドライブレター・ストレージ容量・空き容量を表示します。

| 設定項目 | 内容 |
|---|---|
| デバイス種別 | 選択不可 |
| 対象 | 接続中のドライブ一覧からプルダウンで選択（`C:` / `D:` 等）。直接入力も可 |
| 表示形式 | 「テキスト表示」または「円形グラフ」 |
| メインカラー | ドライブ情報のテキスト色 |
| アクセント | 選択可 |
| 背景アクセント | 選択可 |
| 基準値 | 選択可 |
| クリック動作 | 「表示更新」のみ |

- 円形グラフ形式の場合、グラフの下にドライブレターを表示します。

##### 情報カテゴリ：使用率

対象デバイスの使用率または速度を表示します。

| 設定項目 | 内容 |
|---|---|
| デバイス種別 | CPU / メモリ / GPU / LAN から選択 |
| 対象 | GPU・LAN のみ表示。GPU は `phys_X  GPU名` 形式のプルダウン、LAN はインターフェース名のプルダウンから選択。直接入力も可。CPU・メモリは選択不要のため非表示 |
| 表示形式 | 「テキスト表示」または「円形グラフ」（LAN は円形グラフ選択不可） |
| メインカラー | テキスト・グラフの主要表示色 |
| アクセント | 選択可（LAN 以外） |
| 背景アクセント | 選択可（LAN 以外） |
| 基準値 | 選択可（基準となる使用率の % を数値入力）（LAN 以外） |
| クリック動作 | 「表示更新」のみ |

**デバイス種別別の表示内容**

| デバイス種別 | 表示形式 | 説明 |
|---|---|---|
| CPU | テキスト / 円形グラフ | CPU 全体の使用率（%）。`Win32 Processor` カテゴリの `_Total` インスタンスを使用 |
| メモリ | テキスト / 円形グラフ | 物理メモリの使用量・合計容量（GB）および使用率（%） |
| GPU | テキスト / 円形グラフ | GPU の 3D エンジン使用率（%）。WMI `Win32_PerfFormattedData_GPUPerformanceCounters_GPUEngine` の `engtype_3D` を合計 |
| LAN | テキストのみ | 送受信速度（↓受信 KB/s / ↑送信 KB/s）。`Network Interface` カテゴリの PerformanceCounter を使用。初回読み取り時はカウンターのプライミングのため「↓ - KB/s  ↑ - KB/s」と表示される |

##### 表示形式の種類

**テキスト表示**
- 対象デバイスの情報をテキストで表示します。
- すべての情報カテゴリで選択可能です。

**円形グラフ**
- タイル内に使用率を円形グラフで表示します。
- 情報カテゴリが「ストレージ」または「使用率（CPU・メモリ・GPU）」の場合に選択可能です。
- 使用率カテゴリの LAN は円形グラフに対応しません（テキスト表示のみ）。
- メインカラー：グラフ内の使用率部分に適用。
- アクセントカラー：使用率が基準値を超えた際に、グラフ内の使用率部分に適用。
- 背景アクセント：使用率が基準値を超えた際に、タイル背景色に適用。
- 基準値：アクセント・背景アクセントが適用されるしきい値（% を数値入力）。

#### 起動セクション

種別によって表示される項目が異なります。

| 設定項目 | `app` | `url` | `folder` | `webview` | `system` |
|---|---|---|---|---|---|
| パス / URL | ○ | ○ | ○ | ○ | × |
| 引数 | ○ | × | × | × | × |
| 作業フォルダ | ○ | × | × | × | × |

| 設定項目 | 説明 |
|---|---|
| パス / URL | 起動対象のファイルパスまたは URL |
| 引数 | 起動時に渡すコマンドライン引数（`app` のみ） |
| 作業フォルダ | 起動時のカレントディレクトリ（`app` のみ） |

**D&D 構文（`app` 専用）**

引数フィールドに以下の構文を含めることで、タイルへドラッグ＆ドロップされたファイルをアプリへ引数として渡すことができます。

| 構文 | 説明 |
|---|---|
| `{drop}` | ドロップされたファイルまたはフォルダのパスに置き換えられる。パスは単一・複数問わず常に引用符で囲む。複数アイテムがドロップされた場合は、各パスをスペース区切りで並べる |

引数フィールドに `{drop}` を含む `app` タイルは「D&D 専用タイル」として動作し、通常クリックではアプリを起動しません。

- 通常モードでタイルをクリックしても、アプリは起動しない。
- 通常モードでタイル上にマウスオーバーすると、タイル中央に「**D&D 専用**」というテキストが表示される（通常のホバーアニメーションは行わない）。テキストの背景は角丸半径 10 px の矩形（ページ背景色・透過率 0 %）で、フォントサイズは 12 pt（1×1 タイル幅に収まるサイズ）。
- 通常モードでファイルをタイル上へドロップすると、`{drop}` を実際のファイルパスに置換した引数でアプリを起動する。

#### 見た目セクション

| 設定項目 | 説明 |
|---|---|
| カラー | タイルの背景色 |
| 透過率 | タイルの透明度（[共通仕様参照](#4-共通仕様)） |
| フォント名 | 使用フォント（OS にインストール済みのフォント一覧から選択） |
| フォントサイズ | テキストのサイズ（[共通仕様参照](#4-共通仕様)） |
| フォントカラー | テキストの色 |
| 画像 / アイコンの URL / パス | 表示する画像またはアイコンのファイルパスまたは URL |
| 画像 / アイコン表示位置 | タイル内での画像・アイコンの配置位置（下表参照） |
| 画像 / アイコンを透過させる | チェックON の場合、タイル背景の透過率設定に連動して画像・アイコンも透過する |

**画像 / アイコン表示位置の選択肢**

| 値 | 画像・アイコンの位置 | テキストの位置 |
|---|---|---|
| 上 | タイル上側 | タイル下側 |
| 下 | タイル下側 | タイル上側 |
| 左 | タイル左側 | タイル右側 |
| 右 | タイル右側 | タイル左側 |
| 中心 | タイル全体（テキストと重なる） | タイル全体（画像の前面に描画） |

> 画像とテキストが重なる場合（「中心」指定時など）、テキストは常に画像の前面に描画されます。

### 3.6 カラーパレット

アプリ内で設定可能な色は、事前に定義されたカラーパレットから選択する方式とします。

**初期パレット：**

| カラー名 | カラーコード | 備考 |
|---|---|---|
| blue   | `#4A90D9` | 中程度の青 |
| green  | `#4CAF72` | 落ち着いた緑 |
| purple | `#9B6DD9` | ソフトな紫 |
| orange | `#E8843A` | 温かみのあるオレンジ |
| red    | `#E05252` | 柔らかい赤 |
| gray   | `#8C9BAB` | ブルーグレー |
| teal   | `#2BB5A0` | 青緑（ティール） |
| pink   | `#E868A2` | ビビッドピンク |
| yellow | `#F0C233` | 温かみのある黄 |
| black  | `#2D2D2D` | ソフトブラック（純黒を避け視認性を確保） |
| white  | `#F0F0F0` | オフホワイト（純白を避けグレアを軽減） |

### 3.7 右クリックメニュー

通常モード（ピンモード ON/OFF 問わず）でランチャー本体を右クリックすると表示されるコンテキストメニューです。タスクトレイアイコンを右クリックした場合も同じメニューを表示します。

```
吸着位置の指定
  ├ 上
  ├ 左
  ├ 下
  └ 右
ピンモード切替
──────────────
終了
```

| 項目 | 動作 |
|---|---|
| 吸着位置の指定 | サブメニューから選んだ方向へアニメーションなしで即時吸着し直す（瞬間移動） |
| ピンモード切替 | ピンモードの ON/OFF を切り替える |
| 終了 | アプリケーションを終了する |

**タスクトレイアイコンのクリック動作**

| 操作 | 動作 |
|---|---|
| 左クリック | 何もしない |
| 左ダブルクリック | アプリを最前面に表示してフォーカスし、ピンモードを ON にする。収納モード中の場合は、先に通常モードへの展開アニメーションを再生してからピンモードを ON にする。すでにピンモード ON の場合は、ピンモードを OFF にする |

- 収納モード中にハンドルを右クリックしても、コンテキストメニューは表示されません。
- アプリは Windows タスクバーへのアイコン表示を行いません（タスクトレイのみに表示されます）。
- タスクトレイアイコンは `resources/icons/app.ico` を使用します。同ファイルはコンパイル時に exe へも埋め込まれます。
- 収納モード中にタスクトレイアイコンから吸着方向を変更した場合は、先に通常モードへ展開してから吸着位置を変更します。
- 収納モード中にタスクトレイアイコンからピンモードを ON にした場合は、展開アニメーションを再生しながら通常モードへ移行し、完了後にピンモードを ON にします。
- タスクトレイ常駐が OFF の場合、Alt+F4 などでウィンドウを閉じようとしてもアプリは終了せずウィンドウを最小化します。アプリを終了するには右クリックメニューの「終了」を使用してください。

---

### 3.8 初回起動時の初期設定

アプリを初めて起動したとき（設定ファイルが存在しない場合）は以下の初期状態で起動します。

アプリは前回終了時のモード状態を保存・復元しません。設定ファイルが存在する場合も、常に通常モードで起動します。また、アプリ終了時に確認ダイアログは表示しません。

**全体設定の初期値**

| 項目 | 値 |
|---|---|
| タスクトレイ常駐 | ON |
| Windows 起動時の自動起動 | OFF |
| タイル配置数 | 縦 4 × 横 6 |
| タイル間マージン | 8 px |
| ページ名フォントサイズ | 16 pt |
| 吸着位置 | 右（画面右端の中央） |
| 吸着先モニター | 主モニター（1） |
| 収納待ち時間 | 1000 ms |

**ページ設定**

| 項目 | 値 |
|---|---|
| ページ数 | 4 |
| ページ名 | ページ1・ページ2・ページ3・ページ4（連番） |

**全ページ共通の初期配色**

| 設定項目 | 初期値 |
|---|---|
| ページ背景色 | gray（`#8C9BAB`） |
| ページ背景透過率 | 30 % |
| ハンドル色 | gray（`#8C9BAB`） |
| ハンドル透過率 | 20 % |
| ピンモード枠色 | white（`#F0F0F0`） |
| ピンモード枠透過率 | 50 % |

**ビルド種別による初期タイル配置**

| ビルド種別 | 初期タイル |
|---|---|
| リリース版 | なし（全ページ空） |
| デバッグ版 | ページ1 に以下のタイルを配置（各タイルタイプ・サイズ・機能の動作確認用） |

デバッグ版のページ1 初期タイル（グリッド：縦4 × 横6）：

| グリッド座標 | サイズ | 種類 | 主な設定内容 | 確認目的 |
|---|---|---|---|---|
| (0,0) | 1×1 | `app` | `notepad.exe`・画像位置=上 | app 起動・画像位置（上） |
| (1,0) | 1×1 | `url` | `https://www.google.com`・画像位置=下 | url 起動・画像位置（下） |
| (2,0) | 1×1 | `folder` | `%USERPROFILE%\Desktop`・画像位置=左 | folder 起動・画像位置（左） |
| (3,0) | 1×1 | `folder` | `%USERPROFILE%\Documents`・画像位置=右 | folder 起動・画像位置（右） |
| (4,0) | 1×1 | `system` | CPU 使用率・円形グラフ・更新間隔 1000 ms | system タイル・円形グラフ |
| (5,0) | 1×1 | `system` | メモリ使用率・テキスト・更新間隔 1000 ms | system タイル・テキスト表示 |
| (0,1) | 2×2 | `app` | `calc.exe` | 大タイル（2×2）サイズ確認 |
| (2,1) | 2×1 | `webview` | `https://www.google.com` | webview タイル・横長サイズ |
| (4,1) | 1×2 | `system` | ストレージ C: ・テキスト | system タイル・縦長サイズ |
| (5,1) | 1×1 | `system` | OS 情報・テキスト | OS情報カテゴリ確認 |
| (2,2) | 1×1 | `app` | `notepad.exe`・引数=`{drop}` | D&D 専用タイル動作確認 |
| (3,2) | 1×1 | `app` | `notepad.exe` | 通常 app タイル（比較用） |
| (0,3) | 1×1 | `app` | GIF 画像設定済み（デバッグ用 GIF を配置） | GIF アニメーション確認 |

ページ 2〜4 は空（タイル未配置）。

---

## 4. 共通仕様

### 4.1 透過率

アプリ内のすべての透過率設定に適用される共通ルールです。

- 設定範囲：**0 ～ 90 %**
- 入力方法：スライダーで値を変更する

### 4.2 フォントサイズ

アプリ内のすべてのフォントサイズ設定（タイルのテキスト・ページ名等）に適用される共通ルールです。

- 設定範囲：**6 ～ 72 pt**
