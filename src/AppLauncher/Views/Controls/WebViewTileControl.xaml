<UserControl x:Class="AppLauncher.Views.Controls.WebViewTileControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             Loaded="OnLoaded" Unloaded="OnUnloaded">
    <Grid Background="Transparent">
        <wv2:WebView2 x:Name="WebView" IsHitTestVisible="True"/>

        <!-- 編集モード中 WebView 非表示時のプレースホルダー -->
        <Border x:Name="WebViewPlaceholder" Visibility="Collapsed"
                Background="#CC1C1C2C">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="8">
                <TextBlock Text="WebView" Foreground="#66FFFFFF" FontSize="10"
                           HorizontalAlignment="Center"/>
                <TextBlock x:Name="PlaceholderTitle"
                           Foreground="White" FontSize="13" FontWeight="SemiBold"
                           TextAlignment="Center" TextWrapping="Wrap" Margin="0,4,0,0"/>
                <TextBlock x:Name="PlaceholderUrl"
                           Foreground="#77FFFFFF" FontSize="10"
                           TextAlignment="Center" TextWrapping="Wrap" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>

        <!-- 編集モードオーバーレイ -->
        <Grid x:Name="EditOverlay" Visibility="Collapsed">
            <Border x:Name="EditButton"
                    Width="22" Height="22"
                    HorizontalAlignment="Left" VerticalAlignment="Top"
                    Margin="4" CornerRadius="4" Cursor="Hand"
                    Background="#CC333333">
                <TextBlock Text="✎" FontSize="11" Foreground="White"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <Border x:Name="DeleteButton"
                    Width="22" Height="22"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="4" CornerRadius="4" Cursor="Hand"
                    Background="#CCE05252">
                <TextBlock Text="✕" FontSize="11" Foreground="White"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <Border x:Name="ResizeHandle"
                    Width="16" Height="16"
                    HorizontalAlignment="Right" VerticalAlignment="Bottom"
                    Margin="4" CornerRadius="3" Cursor="SizeNWSE"
                    Background="#CCF0F0F0"/>
        </Grid>
    </Grid>
</UserControl>
